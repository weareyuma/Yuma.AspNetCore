<Project Sdk="Microsoft.NET.Sdk">

   <PropertyGroup>
      <AssemblyName>Yuma.AspNetCore</AssemblyName>
      <RootNamespace>Yuma</RootNamespace>
      <Description>Yuma's Distributed Application Libraries: ASP.NET Core Helpers.</Description>
   </PropertyGroup>

   <PropertyGroup>
      <RepositoryType>git</RepositoryType>
      <RepositoryUrl>https://github.com/weareyuma/Yuma.AspNetCore</RepositoryUrl>
      <PublishRepositoryUrl>true</PublishRepositoryUrl>
      <EmbedUntrackedSources>true</EmbedUntrackedSources>
   </PropertyGroup>

   <PropertyGroup>
      <GeneratePackageOnBuild Condition=" '$(GeneratePackageOnBuild)' == '' ">false</GeneratePackageOnBuild>
      <IsPackable>true</IsPackable>
      <PackageId>$(AssemblyName)</PackageId>
      <PackageDescription>$(Description)</PackageDescription>
      <PackageIcon>yuma-nuget.png</PackageIcon>
      <PackageProjectUrl>$(RepositoryUrl)</PackageProjectUrl>
      <PackageTags>yuma distributed application library</PackageTags>
      <IncludeSymbols>true</IncludeSymbols>
      <SymbolPackageFormat>snupkg</SymbolPackageFormat>
   </PropertyGroup>

   <ItemGroup>
      <InternalsVisibleTo Include="$(AssemblyName).Tests"/>
   </ItemGroup>

   <!-- https://learn.microsoft.com/en-us/nuget/reference/msbuild-targets#packing-an-icon-image-file -->
   <ItemGroup>
      <None Include="..\..\yuma-nuget.png" Pack="true" Visible="false" PackagePath="\"/>
   </ItemGroup>

   <ItemGroup>
      <FrameworkReference Include="Microsoft.AspNetCore.App" Version="2.2.8"/>
   </ItemGroup>

   <ItemGroup>
      <PackageReference Include="MediatR"/>
      <PackageReference Include="Microsoft.SourceLink.GitHub"/>
      <PackageReference Include="Yuma.Persistence.Abstractions"/>
   </ItemGroup>

</Project>
